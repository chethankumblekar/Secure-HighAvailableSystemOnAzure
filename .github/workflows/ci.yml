name: CI

# Placeholder — Phase 3 of the roadmap (see docs/roadmap.md) replaces this
# with: build -> test -> CodeQL SAST -> Trivy image scan -> Syft SBOM ->
# cosign sign -> push to GHCR, all on GitHub's free tier (public repo).

on:

  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build (sample)
        run: echo "Build successful"

      - name: Tests (sample)
        run: echo "Tests passed"
