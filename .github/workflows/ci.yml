name: CI

on:

  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build (sample)
        run: echo "Build successful"

      - name: Tests (sample)
        run: echo "Tests passed"    