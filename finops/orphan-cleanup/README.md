# Orphan-resource cleanup bot

Placeholder — Phase 6 of [the roadmap](../../docs/roadmap.md).

Will port and generalize the cost-calc scripts already written in the
sibling `cloud-control-plane` repo (`finops/cost-calc.sh`,
`finops/cost-model.json`) into a standalone Python + Azure SDK bot that
flags/cleans resources left running outside the apply-demo-destroy pattern.
