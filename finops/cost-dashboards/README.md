# Cost dashboards

Placeholder — Phase 6 of [the roadmap](../../docs/roadmap.md).

Azure Cost Management API data surfaced in Grafana — an actual spend-over-time
graph, not a static estimate, so cost regressions show up the same way a
latency regression would.
