# Backstage golden paths

Placeholder — Phase 7 of [the roadmap](../../docs/roadmap.md).

Will hold Backstage software templates for self-service tenant/service
onboarding — the concrete proof point for "Platform Engineer" rather than
just "DevOps Engineer": a developer fills in a form, the template invokes
the Terraform modules in `infra/terraform/azure` and registers the new
service in ArgoCD.
