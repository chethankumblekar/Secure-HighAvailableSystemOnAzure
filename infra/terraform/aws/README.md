# AWS reference implementation

Placeholder — Phase 1b of [the roadmap](../../../docs/roadmap.md).

Will hold Terraform modules (`vpc`, `eks`, `iam`, `alb`) that stand up an
EKS cluster the same Helm charts and ArgoCD app definitions from
`platform/argocd` can target — proving the platform is cloud-portable, not
just an Azure demo. Azure (`../azure`) is the primary target and is built
first.
